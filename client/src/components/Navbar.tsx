function Navbar() {
    return (
        <header>
            <nav>
                <div>
                    <span>Logo</span>
                </div>

                <div>
                    <button>
                        Enquire Now
                    </button>
                </div>
            </nav>
        </header>
    );
}

export default Navbar;